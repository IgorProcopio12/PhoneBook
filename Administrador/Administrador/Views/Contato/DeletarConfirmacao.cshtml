@model ContatoModel
@{
    ViewData["Title"] = "Confirmação de Exclusão";
}

<div>
    <h3>Deseja apagar o contato (@Model.Nome)?</h3>
</div>

<a class="btn btn-primary d-inline-block" asp-action="Index" asp-controller="Contato" role="button">Cancelar</a>
<a class="btn btn-danger d-inline-block" asp-action="Deletar" asp-route-id="@Model.Id" asp-controller="Contato" role="button">Confirmar Exclusão</a>
